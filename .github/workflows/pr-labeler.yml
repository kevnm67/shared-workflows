name: PR Labeler

on:
  workflow_call:

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Label PR by file paths
        uses: actions/labeler@v6
        with:
          sync-labels: false
